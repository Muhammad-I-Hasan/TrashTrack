import React from 'react';
import "../App.css";
export default function LocationSelector({ children }) {
  return (
    <div style={{
        paddingBottom: '1rem',
        paddingTop: '1rem',
        
    }}>
        {children}
    </div>
  );
}
